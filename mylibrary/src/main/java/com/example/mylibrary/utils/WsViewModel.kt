package com.example.mylibrary.utils

/**
 *  created by ws
 *   on 2021/4/10
 *   describe:
 */
class WsViewModel {
//    mainViewModel = ViewModelProvider(this).get(MainViewModel::class.java)

    // @Volatile
    //    private var mediatorLiveData = MediatorLiveData< MutableList<User>>()
    //mainViewModel.getData()
    //            .observe(viewLifecycleOwner, androidx.lifecycle.Observer<MutableList<User>> { user ->
    //                GlobalScope.launch(Dispatchers.Main) {
    //                    userZ = mainViewModel.getZhujue((context)!!)
    //                    initUser(userZ)
    //                }
    //
    //            })
}